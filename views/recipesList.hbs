<div class="container-fluid">
  <div class="container">
    <h1>Recetas</h1>
    <div class="recipes-wrapper">
      {{#each recipes}}
      {{#with this}}
      {{>recipecard}}
      {{/with}}
      {{/each}}
    </div>
  </div>
</div>